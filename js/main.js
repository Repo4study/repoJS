function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

const names= ['Sara', 'John','Fred']




 



var arr = new Array(10).fill().map((u,index) => ({
author:{
	avatar:'img/avatars/user01.png',
},
offer:{
	title:'title',
  address: 'address',
},
location:{
x: 10.01,
y: 20.02,
}
}
));
console.log(arr)